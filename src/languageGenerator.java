
public class languageGenerator {
/* Construct Words
 * 		get vowel input
 * 		get consonant input
 * 		get tense input (~s, ~ing, ~est, etc)
 * 		get word construction rules
 * 		add to & create ordered list of words
 * 		print random, ordered lists of words
 * Assign words to Parts of Speech
 * Choose Sentence Structure
 * Randomly Generate Sentences
 * */

/*create list of lists: [(A, [])(B, [])(C, [])]
 * ask user to populate: L.get(x).add(y)
 * Ask users for rules: CV(CV)N
 * create functions C, V -> add (x) to string; (~) -> add (x) to string at random
 * */
	
}
