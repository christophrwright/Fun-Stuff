import java.sql.Timestamp;
import java.util.*;


/* Most Awake	||		Downswing
 * -------------||----------------
 * 8-9	  	 	||	 	9:30-11:30 
 * 12-1	 	 	||		1:30-4:30
 * 5:30-6:30 	||		7-9
 */


/*Fun fact: The Olympic legal limit of 12 micrograms per milliliter 
 * (which has been widely adopted for other sporting events) is equivalent
 *  to about six cups of coffee consumed within thirty minutes.*/

public class CoffeeTime {
// get current time currTime
	int wakeUp; //wakeup time
	int currTime = Timestamp();//current time;
	/*range of optimal coffee drinking times; 
	 * if user wakes up at 7:00, [(+ 1:30 :: 3:30); (...)]  */
	int <time>[] = ; 
// generate a set of optimal coffee times, based on wake up time
	if (=(wakeup +))
// if currTime == optimal time +- 15 min, return "Have a cuppa!"
// else if currTime == worstTime +-0 15 min, return "Don't get Coffee!"
// else, return "why don't you wait" + x min + "to get your next cup?"
}
